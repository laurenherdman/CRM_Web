<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Contacts</title>
</head>
<body>
	<h1>All Contacts</h1>

<ul>
<% Contact.all.each do |contact| %>
  <li><%= contact.first_name %> <%= contact.last_name %></li>
  <li><%= contact.email %></li>
  <li><%= contact.notes %></li>
<% end %>
</ul>
</body>
</html>